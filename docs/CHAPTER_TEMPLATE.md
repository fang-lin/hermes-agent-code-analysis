# [编号]-[章节标题：用问题或洞察做标题，不用模块名]

中文 | [English](../en/[对应英文文件名].md)

> **本章定位**：`模块名/` 目录（X 文件，X 行）+ 相关顶层文件。
> **关键类**：`ClassName`（`file.py:行号`）、`function_name()`（`file.py:行号`）。

> **本章基于 hermes-agent commit [`XXXXXXX`](https://github.com/NousResearch/hermes-agent/commit/XXXXXXX)（YYYY-MM-DD）**

---

## [破题：为什么要关心这个子系统？]

<!--
不要以"本章介绍..."开头。用 1-2 段让读者感受到问题的痛感：
- 这个子系统解决的问题是什么？如果没有它会怎样？
- 在整个 hermes-agent 中扮演什么角色？
- 读者读完这章会获得什么认知？

示例（02-agent核心）：
"上一篇架构分析追踪了一条消息的完整路径。但在 Agent 核心那一站，
我们只看了表面。实际上，这个 13,293 行的单文件里藏着大量影响性能、
成本和可靠性的机制……"
-->

---

## 使用指南

<!--
使用指南在前，架构在后。读者先学会用，再理解内部原理。
这部分面向"想用 hermes-agent 的开发者"，语言偏实操。
-->

### 基本用法

[最常见的使用场景，配置示例，命令示例。给出可直接复制运行的代码。]

### 配置

[关键配置项及其含义。不要罗列所有选项——只讲最常用的 3-5 个，其余指向官方文档。]

```yaml
# 示例配置——可直接复制使用
```

### 常见场景

[2-3 个典型使用场景，以具体案例说明。每个场景的结构：场景描述 → 配置/命令 → 预期结果。]

### 排错指引

[常见问题及解决方法。用表格或 Q&A 格式。]

> 📖 **延伸阅读（官方文档）：**
> - [相关功能文档](https://hermes-agent.nousresearch.com/docs/...)
> - [相关参考文档](https://hermes-agent.nousresearch.com/docs/...)

---

## 架构与实现

<!--
架构部分面向"想理解 hermes-agent 内部的开发者"。
核心原则：每一段回答"为什么"，不只是"是什么"。

叙事顺序：
  问题/痛点 → 设计张力 → Hermes 的选择 → 代价和权衡 → 替代方案

每个核心概念自然覆盖 9 问（不需要显式列为小节）：
  是什么、从哪来、在哪里、依赖关系、怎么工作、
  解决什么、替代方案、失败模式、可配置性
-->

### [概念 A：用问题做标题]

<!--
示例标题（好）：
  "Prompt Caching：让重复的 token 不再重复付费"
  "重试与退避：优雅地处理 API 失败"

示例标题（差）：
  "prompt_caching.py 模块"
  "重试机制"
-->

[先讲问题，再讲机制。包含：]
- 问题的痛感（量化：费用、延迟、成功率等）
- 机制的核心算法/流程
- Mermaid 图（流程图、状态图、架构图——视概念性质而定）
- 为什么不用更简单的做法
- 失败时的降级行为
- 用户可配置的参数

### [概念 B]

[同上结构]

### 代码组织

<!--
放在各概念之后，而非之前。
读者已经理解了"为什么"，此时再看代码结构才有意义。
-->

```
模块名/
├── core.py          — [职责]（X 行）
├── helpers.py       — [职责]（X 行）
└── ...
```

### 设计决策

<!--
如果设计决策已经在各概念中展开过了，这里做汇总即可。
每个决策的结构：选择 → 原因 → 代价 → 替代方案。
-->

### 扩展点

[用户/开发者可以如何扩展这个子系统——接口、插件点、配置项]

---

## 与其他章节的关系

[这个子系统如何与其他子系统交互，简要说明，指向相关章节]

---

*本文基于 hermes-agent v0.18.2 源码分析。所有代码引用均经过独立验证。*

---

<!--
章节自检清单（写完后逐项确认，不写入正文）：

开头：
- [ ] 有定位块（代码范围、关键类、commit hash）
- [ ] 有破题段（让读者感受到问题的痛感，而非"本章介绍..."）

使用指南部分：
- [ ] 有可直接复制运行的配置/命令示例
- [ ] 有 2-3 个具体使用场景
- [ ] 有排错指引
- [ ] 有官方文档延伸阅读链接

架构与实现部分：
- [ ] 每个核心概念从问题/痛点出发，不从代码结构出发
- [ ] 有代码组织结构图（含行数）
- [ ] 核心数据结构有说明
- [ ] 有设计决策说明（选择 → 原因 → 代价 → 替代方案）

图表：
- [ ] 每个 ≥3 步流程有 Mermaid 流程图
- [ ] 每个状态转换有状态图
- [ ] 每个组件关系有架构图
- [ ] 每张图有"图：xxx"标题
- [ ] 图上文字有信息量（不只是模块名）

9 问覆盖（每个核心概念）：
- [ ] 是什么（一句话定义 + 类比）
- [ ] 从哪来（设计动机：什么情况下需要这个东西）
- [ ] 在哪里（精确到 文件:行号）
- [ ] 依赖关系（谁创建、谁使用、谁拥有）
- [ ] 怎么工作（核心算法/流程，必须配图）
- [ ] 解决什么（量化收益）
- [ ] 替代方案（为什么不用更简单的做法）
- [ ] 失败模式（出错了会怎样，降级行为）
- [ ] 可配置性（用户能调什么，config.yaml 的哪个字段）

通用检查：
- [ ] 所有关键断言标注了 文件:行号
- [ ] 具体案例标注了"以 X 为例"
- [ ] 概念首次出现时有一句话解释
- [ ] 术语与其他章节一致
- [ ] 去掉所有代码引用后，叙事仍然完整可读
- [ ] 对照 v1 对应章节：v1 覆盖的维度是否全部覆盖
-->
