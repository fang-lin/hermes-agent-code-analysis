# 01 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**

## 工作量评估：高（hermes_cli 文件数翻倍 + 一处上轮刚修的行为又变了）

## A. 过期断言要点

- **hermes_cli/ 97→203 个 .py（+109%）、100,759→163,148 行**（注：新文件里有多少归 09/10 章的 kanban/web_server 范围，重数时排除口径要一致）
- 行号漂移：DEFAULT_CONFIG config.py:503→906；load_config 4377→6655；_deep_merge 4127→6165；_expand_env_vars 4147→6208；migrate_config 3548→5395；PROVIDER_REGISTRY auth.py:183→176；_apply_profile_override main.py:183→340；gateway_command 5006→6332；systemd_install 2440→3071；launchd_install 2900→4053；resolve_alias model_switch.py:450→557；SETUP_SECTIONS setup.py:3053→2603（六步不变 ✓）
- 文件行数：config.py 5,598→8,349；auth.py 7,605→8,275；gateway.py 5,445→7,064；plugins.py 1,725→2,464；model_switch.py 1,799→2,452；setup.py 3,607→3,405（缩了，重构）；main.py 13,847→14,624
- _config_version 23→**33**；顶层配置键约 60→90+（新增 approvals/code_execution/computer_use/cron/curator/desktop/kanban/lsp/moa/network/onboarding 等约 40 个）
- **行为变化（上轮复审刚修的又变了）**："bundled backend/platform 自动加载"→ 现在 **backend 仍自动加载，platform 改为延迟加载（deferred）**：plugins.py 旧 :1042 单条件拆成两支，platform 走 _register_deferred_platform()，首次使用才导入（省 3+ 秒启动）。07 报告的 LoadedPlugin.deferred 双重印证
- VALID_HOOKS 17→23（与 07 报告一致）

## B. 新增机制要点

优先级高：
1. **托管配置作用域**：/etc/hermes/config.yaml（managed scope），leaf 级胜过用户配置——企业集中配置；load_config 缓存签名从 (mtime_ns,size) 扩为 (user_mtime,user_size,managed_mtime,managed_size) + env_snapshot
2. **平台插件延迟加载**（见上，行为变化）
3. **损坏配置自动备份**（config.py:42-93 _backup_corrupt_config → config.yaml.corrupt.<ts>.bak）
4. **env 写入 denylist**（config.py:140-215，约 20 个危险变量名 LD_PRELOAD/PYTHONPATH/PATH 等，防 dashboard env 写入器被利用）
5. **上帝文件分解**：cli_commands_mixin.py（2,736 行）、cli_agent_setup_mixin.py（689 行）、console_engine.py（1,876 行）、container_boot.py（576 行）
6. 进程标题 _set_process_title（ps 显示 hermes）、早期界面检测 _config_default_interface_early / _wants_tui_early（display.interface 配置）

## C. 交叉核对项

- platform 延迟加载 ↔ 07 章 deferred / 08 章平台迁移——三章表述要一致
- 新配置键里 desktop/dashboard_auth ↔ apps 决策
- hermes_cli 文件数暴涨部分来自 09/10 章范围文件，统计口径要与 00 章一致
