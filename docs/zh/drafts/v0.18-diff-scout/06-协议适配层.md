# 06 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改文档时须逐条验证。**
> 侦察时间：2026-07-09

## 工作量评估

需改约 14 处，不止行号漂移——有一处**行为反转**（patch 审批），有 3 个新增机制值得成段/成节覆盖。

## A. 过期断言清单

| 文档行号 | 原文关键句 | 变化 | 新值位置 |
|---------|----------|------|--------|
| 5 | acp_adapter/ 10 个 .py，5,021 行 | 增加 | 11 个 .py，5,288 行 |
| 6 | HermesACPAgent（server.py:445） | 行号漂移 | server.py:450 |
| 6 | EventBridge（mcp_serve.py:204） | 行号漂移 | mcp_serve.py:301 |
| 37 | edit_approval.py，286 行 | 增加 | 338 行 |
| 105/140 | tools.py 1,357 行 | 减少 | 1,291 行（_build_patch_mode_content 被删 66 行；mixture_of_agents 移出 _POLISHED_TOOLS） |
| 126 | 线程池 server.py:85 | 行号漂移 | server.py:90（加了 thread_name_prefix） |
| 127 | ContextVar 隔离 server.py:1501 | 机制升级 | :1561；且从 os.environ["HERMES_INTERACTIVE"] 改为 tools.approval 的 contextvar（修 GHSA-96vc-wcxf-jjff 竞态） |
| 128 | queued_prompts server.py:1323 / :1552 | 行号漂移 | :1332、:1376-1377 |
| 135 | **仅 patch mode=replace 触发审批，V4A 不触发** | **行为反转** | edit_approval.py:183-188：mode="patch"（V4A）现在也触发审批，新增 _proposal_for_patch_v4a() / _extract_v4a_patch_paths()（:131-189）。⚠️ 这是上轮复审刚修过的句子，又变了 |
| 183-195 | 代码组织树各文件行数 | 全体过期 | 重新 wc -l |

## B. 新增机制清单

1. **会话血统追踪（provenance）**：新文件 `acp_adapter/provenance.py`（127 行）——跟踪压缩导致的会话分裂链，ACP 客户端经 `_meta.hermes.sessionProvenance` 拿到当前/前代/根会话 ID 和压缩深度。集成点 server.py:74/717-735/1128/1594。建议新增小节。
2. **实时压缩轮转通知**：turn 内因压缩轮转会话 ID 时发 `session_info_update` 带 provenance 元数据（server.py:1574-1597）。
3. **交互式上下文隔离改进**：HERMES_INTERACTIVE 环境变量 → contextvar（set/reset_hermes_interactive_context，server.py:77-79、1454-1526），修并发竞态（安全公告 GHSA-96vc-wcxf-jjff）。
4. **V4A 补丁审批**（见上表行为反转项）。
5. **记忆工具注入**：inject_memory_provider_tools()（server.py:831/847/1791/1807）。
6. **编辑器 cwd 传播**：agent.session_cwd（session.py:660）。
7. **中断响应抑制**：内部中断消息不再发给 ACP 客户端，改用 stop_reason（server.py:1600-1609）。
8. **会话持久化精细化**：_session_db_created 标志区分持久化归属（session.py:464-504）。
9. **MCP 路由索引迁移 state.db**：_load_sessions_index 优先读 state.db，sessions.json 降为回退，消除 dual-file 竞态 #8925（mcp_serve.py:82-168/442-471）；轮询只查 state.db mtime（:316/355-365）。
10. acp_registry/agent.json 版本 v0.14.0 → v0.18.2。

## C. 更新要点（给修改时用）

- 统计数字全换（头部、代码树）
- patch 审批一段重写（行为反转 + V4A 提案函数）
- ContextVar 段更新为 contextvar 机制 + 提安全公告
- MCP Serve 节补 state.db 索引迁移 + 轮询优化
- 新增"会话血统追踪"小节（provenance.py）
