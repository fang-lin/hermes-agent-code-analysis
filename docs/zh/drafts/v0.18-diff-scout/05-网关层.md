# 05 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**
> ⚠️ 本报告有一处重大存疑（平台"删除"），见 C 节，未核实前不得写入文档。

## 工作量评估：高（GatewayRunner 拆成 mixin + 平台目录大调 + 流式模型升级）

## A. 过期断言要点

- gateway/run.py 18,318→20,719 行；GatewayRunner 1547→2774；_agent_cache 1649→2961；busy_input_mode 1558→2785；pre_gateway_dispatch 6544→8754；_platform_reconnect_watcher 5521→7717
- 缓存上限 128 / TTL 3600 不变 ✓（位置微移 :67/:70）
- BasePlatformAdapter base.py:1389→2253（base.py 4,126→5,627 行）；SessionStore session.py:668→961；get_or_create_session 856→1687
- stream_consumer.py 1,318→1,800
- **_is_user_authorized 从 run.py 迁出**：GatewayRunner 改为混入 3 个 mixin——authz_mixin.py（710 行）/ kanban_watchers.py（1,286 行）/ slash_commands.py（4,624 行）
- gateway/ 文件数 61→72

## B. 新增机制要点

优先级高（影响本章骨架）：
1. **mixin 分层重组**：单体 run.py → GatewayRunner + 3 mixin，"代码组织"节要重写
2. **handle_message 并发模型重写**：活跃会话检查 + session 级 interrupt guard + background task + pending 队列，支持运行中拦截 /stop /new /reset——**9 步消息路径图要重绘**
3. **流式事件模型升级**：粗粒度 token delta → MessageChunk/ToolCallChunk/Commentary 细粒度事件（stream_events.py 171 行），适配器可自定义渲染
4. **draft streaming**：send_draft/supports_draft_streaming 等 4 新方法（Telegram Bot API 9.5+ 实时草稿预览）
5. **能力宣告属性**：适配器新增 5 个类级属性（supports_async_delivery/splits_long_messages 等）；connect() 新增 is_reconnect 参数
6. **delivery router**（delivery.py 258 行）：按平台能力决定长消息在哪层分割
7. **瞬时网络错误自愈**（run.py:220-280）：event loop 级异常捕获，Telegram timeout 不再拖垮 gateway
8. **relay 子包**（gateway/relay/，~2,400 行）：Team Gateway WebSocket connector，authorization_is_upstream 上游信任
9. 响应 sanitization（secret redaction 集成）、raw text 平台分类（_GATEWAY_RAW_TEXT_PLATFORMS）、busy_text_mode 兼容加载器

## C. ⚠️ 重大存疑：平台"删除"清单

agent 称 Telegram/Slack/Matrix/飞书/钉钉/Email/WhatsApp/Mattermost/企业微信/HomeAssistant/SMS **"已完全删除"**、平台从 39→26 文件、"新版最大是 yuanbao.py"。**Telegram 被删极不合理**（旗舰平台，且 B 节 draft streaming 就是给 Telegram 做的）。最可能的解释：**平台适配器迁移到了 plugins/platforms/**（agent 自己声明没查 plugins 目录）。
→ **必须与 08 章（内置插件）侦察结果交叉核对后再下结论**；"约 19 核心 + 7 插件"的平台分布断言在两份报告合并前不动。
