# 10 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**

## 工作量评估：高（本章受 desktop 拉动，是重灾区）

## A. 过期断言要点

- **行数**：cli.py 14,785→16,184；main.py 13,847→14,624；**web_server.py 4,671→16,926（+262%，扩成 desktop 后端）**；voice_mode 1,129→1,218；tts_tool 2,369→2,870；transcription_tools 963→1,799（+87%）；display.py 1,037→1,440；skin_engine 926 不变
- **tui_gateway**：8→11 文件（新增 git_probe.py 193 行 / loop_noise.py 83 行 / project_tree.py 558 行，都为 desktop 侧边栏服务）
- **cli.py 行号全体漂移约 +600~+1,240**：process_loop 14003→15071、chat 11227→11990、_render_final_assistant_content 1858→2454、main 14426→15669、信号处理 14142→15262-15378；模态状态机 12228→4044-4066（agent 称"上移"，可疑，需核实）
- **STT 回退链扩展**：local>groq>openai>xai → **local>local_command>groq>openai>mistral>xai>elevenlabs**（transcription_tools.py:1628-1630，新增 mistral/elevenlabs）
- **TTS 新增 provider**：xai/gemini/minimax/kittentts/自定义 command
- 皮肤仍 9 套 ✓；SPINNERS 报告自相矛盾（一处说仍 9 个、一处说 7→9），需核实；KAWAII_THINKING 10→15（也与旧文档"15 动词"区分开，动词仍 15）
- 流式 TTS 仍仅 ElevenLabs ✓；Ctrl+B ✓；蜂鸣频率未验证

## B. 新增机制要点

1. **web_server.py 变成 desktop 后端**（最大变化，或需专门小节/与 apps 决策联动）：HERMES_DESKTOP=1 模式、/api/pty WebSocket（PTY 通道）、desktop 内嵌 cron ticker（不依赖 gateway，web_server.py:118-136）、事件发布订阅（/api/pub、/api/events）、console WebSocket、插件管理 API 6 端点、主题/字体持久化、用量统计 API
2. tui_gateway server.py 大扩展：git_probe RPC、PTY_REGISTRY、_SlashWorker 扩展
3. HERMES_TUI_GATEWAY_URL thin-client 模式产品化
4. 语音：Termux（Android）支持、PulseAudio/PipeWire 检测、SSH 远程音频回退
5. display.py：waiting/thinking faces 和动词改由皮肤配置读取（不再硬编码）
6. Ink 5→6.8、React 19.2.4、ui-tui 新增 30+ 测试文件

## C. 联动与风险

- **web_server 的膨胀是 desktop 拉动的**——本章"Web Dashboard"节的边界要跟 apps/ 决策一起定
- agent 的"80-120 小时"类估时不可信，忽略；按段落数估：本章约 8-10 处行号 + 2 个整节级补充/重写
