# 03 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**

## 工作量评估：中高（数字全变 + 约 18 个新机制要选材）

## A. 过期断言要点

- 注册工具 **72→69**（agent 用 AST 扫描数的，更新时用与之前一致的 grep 口径复核）
- tools/ 顶层 .py **96→93**
- DANGEROUS_PATTERNS **52→57**（新增 Windows PowerShell 破坏命令、killall 模式等 14 个，位置移到 :547-762）
- HARDLINE_PATTERNS：agent 称新版只有 **5** 个（文档写"12 类"——旧值是否真是 12 需核对旧代码，可能是首轮遗留错误）
- 执行后端 **8→7**：`vercel_sandbox.py` 被删
- computer_use **6→8** 文件：新增 doctor.py（288 行）、permissions.py（200 行）
- approval.py **1,424→3,242 行（+128%）**；check_all_command_guards 1043→2537；Gateway 审批超时逻辑移到 ~2687
- model_tools.py 923→1,375；钩子行号全变：pre_tool_call 1175、post_tool_call 968/1194/1294（改为专用发射函数 _emit_post_tool_call_hook）、transform_tool_result 1317
- _HERMES_CORE_TOOLS 48→**49**
- terminal_tool.py 2,405→约 3,000+
- _PARALLEL_SAFE_TOOLS 位置变动，数量待核

## B. 新增机制要点（约 18 个，更新时选重要的成段）

优先级高（改变本章叙事的）：
1. **异步委托**：`delegate_task(background=true)` 后台子 Agent，tools/async_delegation.py（531 行）+ daemon_pool.py——与 02 章子 Agent 内容联动
2. **write_approval.py**（493 行）：文件写入审批（补充命令审批之外的第二套审批）——"安全三道防线"叙事要扩
3. **approval.py 检查体系重构**：六级检查加 cron_mode/session state 上下文感知（:2537-2650）
4. **tool_search.py**（735 行）：工具搜索引擎
5. **threat_patterns.py**（284 行）：威胁模式统一库
6. GUI 终端工具 close_terminal/read_terminal（需 HERMES_DESKTOP）——与 desktop 联动
7. mcp_stdio_watchdog.py（184 行）：MCP 子进程看门狗

优先级中低：hook 存在性检查 has_hook、skip_pre_tool_call_hook、env_probe、blueprints、hook_output_spill、thread_context、xai_video_tools、computer_use doctor
另：agent/tool_executor.py 912→1,646 行（预算管理/持久化/中断处理）

## C. 交叉核对项

- **moa/mixture_of_agents 工具是否被整体删除**（12 章分布移除 moa、06 章 _POLISHED_TOOLS 移除 mixture_of_agents 都指向这一点）——更新 03 时先定论
- Vercel Sandbox 删除是否有官方说明（改走 MCP？）
