# 07 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**
> ⚠️ 报告内有数字自相矛盾处，见 C 节。

## 工作量评估：中高（钩子/方法数大增 + 平台插件翻近三倍）

## A. 过期断言要点

- plugins/ 总量：122 个 .py / 46,477 行 / 16 类 → **177 个 .py / 104,898 行 / 18 类**（新增 cron_providers、security-guidance、dashboard_auth——但 16+3=19 与"18 类"矛盾，重数）
- **VALID_HOOKS 17→23 种**（位置 :128→:135-213）。agent 列的"新增"清单里混进了旧版已有的 pre/post_approval_request——它自称"新增 9 种"但其中 2 个旧版就有，实际新增待重数。真正的新钩子方向：pre_verify（验证循环门控）、api_request_error、subagent_start、kanban_task_claimed/completed/blocked
- **PluginContext 14→24 个方法**（:287→:337）：新增 register_tts_provider / register_transcription_provider / register_dashboard_auth_provider / register_secret_source / register_slack_action_handler / register_middleware（6 个，14+6=20 与"24"矛盾，重数）
- 类移位：PluginManifest 234→279；PluginManager 883→1246；register_tool 317→389；register_hook 814→1156；dispatch_tool 468→583（方法行号表全体过期）
- MemoryProvider ABC 18→19 方法（新增 backup_paths()）
- **平台插件 7→20 个**：新增 dingtalk/feishu/email/homeassistant/line/matrix/mattermost/ntfy/photon/raft/simplex/sms/wecom/whatsapp——⚠️ line/ntfy/simplex 旧版就有（在旧 7 个清单里），agent 的"新增"清单又混了旧项，实际是从 gateway/platforms/ **迁移进来**一批 + 新增 photon/raft 等，精确清单待与 08 报告合并后重数
- 行数：hermes_cli/plugins.py 1,725→2,464；memory_manager.py 533→1,086；memory_provider.py 38→315（**旧值 38 可疑**——旧版 ABC 在 :42 不可能只有 38 行，重测）；plugins/memory/__init__.py 53→450（**旧值 53 同样可疑**，旧 _ProviderCollector 在 :288）

## B. 新增机制要点

1. **中间件系统**（invoke_middleware/has_middleware，plugins.py:1935-1956）：与钩子并行的新扩展面——可能值得成节
2. **延迟加载**（LoadedPlugin.deferred，:329）：platform 插件懒加载，避免启动导入重型 SDK——与平台大迁移直接相关（迁移的前提）
3. 新钩子组：pre_verify（代码编辑后验证门控）/ subagent_start / api_request_error / kanban_task_* 三件套
4. PluginContext 新注册面：TTS/转录/Dashboard 认证/密钥源/Slack 动作/中间件
5. MemoryProvider.backup_paths()
6. 新类别 cron_providers（对应 11 章 scheduler provider 接口）、security-guidance、dashboard_auth（对应 desktop）

## C. 与其他章的交叉核对

- **平台迁移已基本证实**（联动 05 章）：gateway/platforms/ 里"消失"的 feishu/matrix/wecom/sms/dingtalk/email/whatsapp/mattermost/homeassistant → plugins/platforms/。**Telegram 不在迁移清单中**——05 报告称 Telegram 被删仍未解释，等 08 报告确认 telegram 在哪
- cron_providers 类别 ↔ 11 章 scheduler_provider.py
- dashboard_auth ↔ desktop/10 章
- ⚠️ agent 报告的三处数字矛盾（类别数 18 vs 19、方法数 24 vs 20、memory_provider 旧行数）更新时用脚本重数
