# 12 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改文档时须逐条验证。**
> 侦察时间：2026-07-09

## 工作量评估

中等修订（原判"几乎零改动"不成立）。batch_runner.py 零改动、CompressionConfig/--model 默认值/think 块逻辑都没变，但 moa 移除改变了分布描述，压缩器新增了边界对齐机制。

## A. 过期断言清单

| 文档行号 | 原文关键句 | 变化 | 新值 |
|---------|----------|------|------|
| 5 | trajectory_compressor.py 1,508 行 | +66 | 1,574 行 |
| 5 | mini_swe_runner.py 735 行 | -3 | 732 行（logging.basicConfig 从 __init__ 移到 main，:671-680） |
| 5 | toolset_distributions.py 364 行 | -6 | 358 行 |
| 207 | DISTRIBUTIONS 字典 :29-220 | 缩短 | :29-214（**总数是否仍 17 种需主线核实**——moa 是从分布里移除工具集项，不是删分布，但要数一遍） |
| 207 | research 含 moa 40 | **moa 全局移除** | 现 web 90/browser 70/vision 50/terminal 10（:55-62） |
| 207 | development 含 moa 60% | 同上 | 现 terminal 80/file 80/web 30/vision 10（:79-87） |
| 207 | reasoning 分布（moa 90/web 30/terminal 20） | **完全重构** | 现 web 90/file 60/terminal 20，描述从"Heavy mixture of agents"改为"Heavy research/reasoning"（:151-160） |
| 226 | compress_trajectory() :709 | 行号漂移 | :743；另新增 compress_trajectory_async() :879 |

**moa 移除范围**：default/image_gen/research/science/development/safe/balanced/reasoning 共 8 个分布的 moa 项全部删除。⚠️ 这暗示 moa 工具本身可能被弃用/重构——**要与 03 章（工具系统）侦察结果交叉核对**。

## B. 新增机制清单

1. **压缩边界对齐**（值得成段覆盖）：`_is_boundary_clean()`（:526-536）+ `_snap_boundary()`（:539-562）——防止压缩区间切在 gpt→tool 消息对中间产生孤立 tool 消息；边界向前吸附，找不到则后退；若 snap 后区域塌陷（compress_until <= compress_start）直接返回原轨迹并置 still_over_limit=True（:826-835、941-950）。应用点 :786/:826/:909/:941。
2. **compress_trajectory_async()**（:879）：异步版本。
3. **agent_runtime_helpers.py 消息序列修复 Pass 0**（:405-473）：合并连续 assistant 消息——严格 OpenAI 兼容 provider（DeepSeek v4、Kimi）会拒绝连续 assistant；_is_codex_interim()（:423-429）豁免 Codex Responses 中间轮次。对轨迹生成鲁棒性有影响，可选补充。
4. agent_runtime_helpers 新增 AGENT_RUNTIME_POST_HOOK_TOOL_NAMES / _MAX_AUTH_REFRESH_ATTEMPTS=2（可选，非本章核心）。

## C. 更新要点

- 确认不变（可保留）：batch_runner.py 全部内容、CompressionConfig 默认值、CLI --model 默认值、think 块归一化
- 必改：3 处行数、分布描述（moa 移除 + reasoning 重构）、compress_trajectory 行号
- 建议新增：_snap_boundary 边界对齐一小节（配图）
- 交叉核对：moa 弃用与 03 章工具清单的一致性
