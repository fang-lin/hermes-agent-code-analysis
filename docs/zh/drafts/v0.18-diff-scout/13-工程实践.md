# 13 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**

## 工作量评估：高（hermes_state 翻倍 + 两处行为级变化）

## A. 过期断言要点

- 行数：hermes_state.py 3,279→6,409；hermes_logging.py 389→789；utils.py 361→546；tests/ .py 1,208→2,017（test_*.py 1,960）
- 行号：SessionDB 311→871；_execute_write 377→1142；_parse_schema_columns 466→1259；_reconcile_columns 508→1301；_ComponentFilter 126→219
- SCHEMA_VERSION 13→**19**（v16：_delegate_from 子代理级联删除标记；v18：gateway 元数据回填 sessions.json→state.db；v14/15/17/19 纯声明式列协调——"加列归声明式、复杂变更归版本链"的叙事仍成立且有了新例证）
- **行为变化 1：WAL checkpoint PASSIVE→TRUNCATE**（:1196-1225，会截断 WAL 文件防膨胀）——上轮文档"每 50 写 PASSIVE checkpoint"过期
- **行为变化 2：日志三路→四路**：新增 gui.log（10 MiB×5，MODE=gui 分流 web_server/pty_bridge/tui_gateway/uvicorn）——对应 desktop
- CI：硬编码 slice [1..6] → 可配 slice_count（默认 **8**），workflow_call 复用，uv sync --locked
- NFS 标记：删了 "disk i/o error" 一项（_WAL_INCOMPAT_MARKERS 变短）

## B. 新增机制要点

优先级高：
1. **compression_locks 表**（:783-788）：会话压缩并发锁——新表，7 张表？（连同 09 章 task_attachments，各章表数都要重数）
2. **FTS 段优化**（:1227-1241）：每 1000 写 PRAGMA optimize（_OPTIMIZE_EVERY_N_WRITES）
3. **MAX_FTS5_QUERY_CHARS=2048**（:127-130）
4. **utils 原子写增强**：atomic_json_write 新 mode 参数（fchmod 原子设权限，secret 文件）、EXDEV/EBUSY copy-fallback（跨设备/bind-mount）、symlink 所有权保留（_preserve/_restore_file_owner，Docker/NAS）
5. **Windows concurrent-log-handler**（多进程 rename 争用）+ _ManagedRotatingFileHandler（NixOS/Docker 权限保留）
6. 新会话列一批（session_key/chat_id/thread_id/cwd/git_branch/rewind_count/archived 等 14 列）、Telegram topic 迁移（:5625+）
7. conftest 凭证清单扩到 150+ 变量；per-file subprocess 注释补了 xdist 对比数据（17k 测试 70min CPU→3.5min wall）
8. pydantic 2.12.5→2.13.4（跨线程 segfault #40503）；lazy_deps.py 613→970 行

## C. 交叉核对项

- gui.log ↔ 10 章/desktop；_delegate_from ↔ 02 章子 Agent；compression_locks ↔ 02 章压缩
- COMPONENT_PREFIXES 新增 "plugins.platforms"（#41112）——平台插件迁移的又一佐证（联动 05/07/08）
