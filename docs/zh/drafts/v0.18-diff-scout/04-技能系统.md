# 04 章 diff 侦察报告（3bace071b → v2026.7.7.2）

> 侦察 agent 输出的工作底稿。**行号/数字未经主线核实，修改时逐条验证。**

## 工作量评估：中高（技能库结构大调 + 触发逻辑迁移新文件）

## A. 过期断言要点

- **内置技能 89→72 个 SKILL.md，分类 20→18**（删 diagramming/domain/gaming/gifs/inference-sh/mcp/red-teaming 等，新增 computer-use）
- **可选技能 81→102 个，分类 17→20**（新增 docker-management/external-tools/llm-routers）
- 行数：skills_tool.py 1,567→1,681；skill_manager_tool.py 1,034→1,559（+51%）；background_review.py 593→960（+62%）；hermes_cli/skills_hub.py 1,617→1,997
- skill_manage() 816→1,320；spawn_background_review_thread() 558→925
- **触发条件迁移新文件**：conversation_loop.py:4178-4183 → **turn_finalizer.py:456-460（新文件）**；计数递增 :701-703→:697；计数重置 tool_executor.py:94→:344/:1067
- **缓存六元组→七元组**（prompt_builder.py:1458-1466，新增 compact_categories）
- Curator：should_run_now 199→219；maybe_run_curator 1763→1958；默认值区 56-59→56-64（新增 DEFAULT_CONSOLIDATE=False）

## B. 新增机制要点（8 项）

优先级高：
1. **审查模型路由**（background_review.py:46-100 _resolve_review_runtime）：auxiliary.background_review.{provider,model} 可把后台审查派给别的模型；同模型→复用 prompt cache 全量重放，异模型→降级 DIGEST 紧凑模式省 token。值得成节（"后台审查的模型选择与成本"）
2. **Curator 两种模式**：consolidate 开关（纯确定性清理 vs LLM 合并策展，默认关）+ prune_builtins（内置技能也可归档，归档后 hermes update 不会重装回来）
3. **删除安全检查**（skill_manager_tool.py:156-210）：_is_path_redirect/_validate_delete_target 防 symlink 重定向/删根/删树外路径（防误删用户目录）
4. **cron 引用保护**（curator.py:276-290 _cron_referenced_skills）：被定时任务引用的技能不被自动归档
5. **compact_categories**：系统提示里指定分类的技能只显示名称+描述（编码类降噪）

优先级中：review_memory/review_skills 独立开关；多 Profile 技能目录动态解析（_skills_dir，长驻进程按 session 的 profile 解析）；skill_provenance 重构

## C. 注意

- 技能数量变化大（89→72 / 81→102），附录"170 技能索引"类内容要整体重列
- turn_finalizer.py 是新文件——02 章（对话循环）侦察结果出来后交叉核对该文件归属叙述
