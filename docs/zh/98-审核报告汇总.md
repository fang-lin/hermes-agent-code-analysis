# 审核报告汇总

中文 | [English](../en/98-review-report.md)

本文档汇总各章节审核的统计数据和关键发现。

---

## 第 00 章：项目全景

### 事实审核

- 检查断言数：41
- ✅ 通过：31
- ⚠️ 存疑：5
- ❌ 错误：5
- 二次验证：8 项中 7 项确认采纳，1 项推翻（注册工具数 72 经验证正确）

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| 核心 PyPI 依赖 | 12 个 | 16 个 |
| 执行后端 | 7 种 | 8 种（加 ManagedModal）|
| 可选 extras | 20+ | 35 |
| 插件类别 | 17 | 16 |
| Claude Co-Author | 246 次 | 288 次 |
| Hermes Co-Author | 23 次 | 26 次 |
| Python 源文件 | 624 | 617 |
| 测试文件 | 1215 | 1208 |

### 文学性审核

- 改进建议数：9（必须改 4 / 建议改 5）
- 全部采纳

主要改进：
- 开头过渡从假承诺改为"先用后理解"契约
- ACP/FTS5 首次出现时加定义
- 比喻与数字分开，避免语体摩擦
- 多处段间过渡补充
- dogfooding 加解释

### 种子测试（审核 Agent 有效性验证）

- 埋入事实错误 4 个 → 全部抓到（4/4）
- 埋入文学性问题 3 个 → 全部抓到（3/3）
- 结论：两个 sonnet 审核 Agent 有效

---

## 第 01 章：基础设施层

### 事实审核

- 检查断言数：32
- ✅ 通过：22
- ⚠️ 存疑：4
- ❌ 错误：6
- 二次验证：6 项全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| DEFAULT_CONFIG 大小 | 约 200 行，约 15 个顶层分组 | 1289 行，约 60 个顶层 key |
| 认证方式数 | 5 种 | 6 种（加 external_process） |
| 内置皮肤 | 6 个（名字错误） | 9 个（default/ares/mono/slate 等） |
| model_aliases 来源 | 独立 YAML 文件 | config.yaml model_aliases: 节 |
| _model_flow_* 数量 | 约 30 个 | 约 19 个 |
| VALID_HOOKS | 18 种 | 17 种 |

### 文学性审核

- 改进建议数：9（必须改 4 / 建议改 5）
- 全部采纳

主要改进：
- Layer 间过渡句补充（3 处）
- 回调桥接和 Kanban 的引入过渡
- ProviderConfig、api_mode 首次出现加解释
- plugins 信息密度降低

---

## 第 02 章：Agent 核心

### 事实审核

- 检查断言数：22
- ✅ 通过：16
- ⚠️ 存疑：4
- ❌ 错误：2

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| 最终兜底上下文 | 128K | 256K |
| fallback 链行号 | 1229-1426 | 从 1430 行开始 |
| cache_ttl | "5min" | "5m" |
| agent/ 文件数 | 78 个 | 102 个（含子目录）|

### 文学性审核

- 改进建议数：14（必须改 5 / 建议改 9）
- 全部采纳

主要改进：
- 机制间过渡句 ×4
- FailoverReason/ClassifiedError 加定义
- Trajectory 段落扩展
- Fallback 术语统一

---

## 第 03 章：工具系统

### 深度审核（v2 补跑）

- 补充内容：钩子链执行顺序、并行四规则细节、安全防线实现、MCP 架构、终端 debug 方法

### 事实审核

- 检查断言数：15
- ✅ 通过：11
- ❌ 错误：4（数字错误 + 行号错误）
- 二次验证：4 项全部确认采纳
- 增量复核：5 条（4✅ 1 误报——DANGEROUS_PATTERNS 46 vs 47，主线确认 47 正确）

### 文学性审核

- 已执行，采纳所有建议

---

## 第 04 章：技能系统

### 深度审核

- 检查机制数：7
- 🟢 深度足够：1
- 🟡 部分缺失：3
- 🔴 严重不足：3

主要补充：
- 新增 Curator 生命周期系统（四种状态 + pinned 标志 + .usage.json 遥测）
- review Agent 触发条件从简化描述扩展为完整四前置条件
- frontmatter 字段表扩展（conditions 四子类型 + readiness_status 三态）
- 缓存失效机制修正（磁盘层 mtime-aware vs 内存层需重启）
- 排错表补充静默过滤场景

### 文学性审核

- 改进建议数：8（必须改 4 / 建议改 4）
- 全部采纳

主要改进：
- 破题段过渡到使用指南的预告句
- "技能怎么自改进"从两句话扩展为三种 action 详解
- "fork"改为"daemon 线程"避免 Unix 歧义
- 删除 skill_provenance.py 重复段落

### 事实审核

- 检查断言数：32
- ✅ 通过：23
- ⚠️ 存疑：7
- ❌ 错误：2
- 二次验证：全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| conditions 字段路径 | 顶层 `conditions:` | `metadata.hermes.*` 下 |
| .usage.json 字段名 | `agent_created` | `created_by`（值为 `"agent"`） |
| 分类数 | 25 个 | 20 个含技能分类 |
| pinned | 第四种状态 | 布尔标志，与状态正交 |
| 排错表入口函数 | `build_skills_system_prompt()` | `spawn_background_review_thread()` |

---

## 第 05 章：网关层

> 注：本章 v1 期间完成（步骤 6.0，原编号 06-Gateway），v2 仅重命名。

### 事实审核

- 发现：asyncio.to_thread 描述不准确、fresh-final 来源错误、PII 遗漏 2 个平台、/steer 不是斜杠命令
- 全部修正

### 文学性审核

- 已执行，采纳所有建议

---

## 第 06 章：协议适配层

> 注：本章 v1 期间完成（步骤 8.0，原编号 08-Cron 调度与外部协议），v2 仅重命名。

### 事实审核

- 整体 ✅，对比表"纯数据桥"矛盾修正

### 文学性审核

- 四机制过渡句、术语统一、MCP 场景提前

---

## 第 07 章：插件框架

> 注：本章 v1 期间完成（步骤 5.0，原编号 05-插件系统），v2 仅重命名。

### 事实审核

- 发现 1 ❌（指数→线性退避）+ 遗漏（register_skill、entry-point）
- 全部修正

### 文学性审核

- 1 🔴（Honcho 段密度）
- 全部采纳

---

## 第 08 章：内置插件

### 深度审核

- 检查维度：3（能否重新实现 / 能否调试 / 流程是否完整）
- 补充内容：model-providers 两层设计拆解（声明式 vs 覆写）、google_meet 5 个工具名列举、kanban 调度器位置澄清

### 文学性审核

- 改进建议数：6（必须改 2 / 建议改 4）
- 全部采纳

### 事实审核

- 检查断言数：28
- ✅ 通过：18
- ⚠️ 存疑：5
- ❌ 错误：5
- 二次验证：5 项全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| memory 行数总计 | 12,054 | 11,647 |
| hindsight 行数 | ~600 | 1,757 |
| holographic 行数 | ~800 | 1,782 |
| mem0 行数 | ~700 | 373 |
| Discord 行数 | ~5,000 | 6,195 |
| Google Chat 行数 | ~2,000 | 3,984 |
| Teams 行数 | ~2,000 | 1,200 |
| IRC 行数 | ~1,500 | 972 |
| google_meet 工具名 | meet_start/meet_stop | meet_join/meet_status/meet_transcript/meet_leave/meet_say |
| 简单 Provider 示例 | DeepSeek（40-50 行） | Alibaba（13 行） |
| kanban 关联表 | "Agent 侧调度器" | "Dashboard UI；调度器在 gateway" |

---

## 阶段 3 完整性审核（07-08 完成后）

- 检查范围：第 07-08 章，参考 00-06 章跨章检查
- 源码覆盖率：plugins/ 约 90%
- 跨章不一致：4 处
- 失效引用：2 处
- 9 问跨章缺口：3 个概念

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| 08 章钩子数 | 18 种 | 17 种 |
| 07 章 MemoryProvider 方法名 | build_system_prompt / prefetch_all / sync_all | system_prompt_block / prefetch / sync_turn |
| 05/06/07 章标题编号 | 04/05/06 | 05/06/07（对齐文件名） |
| 07 章跨章引用 | "04 章"（网关层） | "05 章" |
| 08 章 browser 后端 | "以 Browser Use 为例" | 3 个后端（Browser Use/Browserbase/Firecrawl） |
| 08 章 meet_say | 未标注限制 | 标注依赖 v2 realtime 模式 |

待改进（非阻塞）：
- 07 章可补充 Gateway hooks / Shell hooks（三钩子体系目前只覆盖 Plugin hooks）
- 07/08 章可补充 `ctx.llm.complete()` API 接口说明
- 08 章 web/ 后端列举可补全（8 个中只列了 4 个）

---

## 第 09 章：Kanban 系统

### 深度审核

- 检查机制数：8
- 🟢 深度足够：2
- 🟡 部分缺失：3
- 🔴 严重不足：3

主要补充：
- Respawn Guard 三条防抖规则（blocker_auth/recent_success/active_pr + 时间窗口）
- Sticky block vs 自动阻塞的区别（blocked 事件 vs gave_up 事件）
- review 状态真实进入路径（非 Worker 工具触发）
- scheduled 状态完整语义（与 Cron 集成点）
- _kanban_notifier_watcher（平台消息推送，5 秒轮询）
- complete 幻觉检测（created_cards 验证 + 散文扫描）
- Worker 日志路径和环境变量表扩展（6→12 个）

### 文学性审核

- 改进建议数：9（必须改 4 / 建议改 5）
- 采纳关键 4 个

主要改进：
- 开篇比喻从人类团队改为 Agent 分工
- review 状态描述修正

### 事实审核

- 检查断言数：32
- ✅ 通过：23
- ⚠️ 存疑：3
- ❌ 错误：6
- 二次验证：全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| review 触发方式 | `kanban promote --status review` | 不存在此命令，需直接 DB/自动化 |
| 工具权限表 | complete/block/heartbeat 编排者 ❌ | 编排者 ✅（check_fn 是 kanban_mode） |
| kanban_link | 缺失 | 补入表格 |
| Board 解析优先级 | board= 参数最高 | HERMES_KANBAN_DB 最高 |
| current 文件 | 符号链接 | 一行文本文件 |
| scheduled 行号 | 3879（schedule_task） | 3377（unblock_task） |

---

## 第 10 章：交互界面与运行模式

### 深度审核

- 检查机制数：9
- 🟢 深度足够：2
- 🟡 部分缺失：5
- 🔴 严重不足：2

主要补充（二次验证全部确认）：
- tui_gateway panic hook 崩溃取证（`server.py:37-107`，`~/.hermes/logs/tui_gateway_crash.log` + sys/threading 双 excepthook）
- 一次完整 turn 的生命周期（输入预处理 4 步 / chat() 内部 turn 配置重解析+图像路由+@context / turn 后目标延续+语音+通知）+ 新增流程图
- 经典 TUI 输入框的模态状态机（5 种：clarify/sudo/approval/slash_confirm/secret，各带 response_queue + deadline）
- SIGTERM/退出/清理链（interrupt→grace→app.exit 而非 raise KBI，issue #13710；logger.debug 防重入）
- cmd_chat() 预处理（-c 双来源解析、首次运行守卫、--yolo）
- SkinConfig 补 description/banner_hero 字段 + completion_menu/selection/voice 色槽组
- **拒绝 1 处误报**：审核 Agent 据过时模块注释称「6 套皮肤」，grep 实际字典键确认为 9 套，草稿正确

### 文学性审核

- 改进建议数：12（必须改 4 / 建议改 8）
- 全部采纳

主要改进：
- 删孤立段落「但它不是。」并入上下文
- SIGTERM 节加过渡句衔接「三步」到「logger.debug 细节」
- 模态状态机的设计动机（为何不用 input()）上移到表格前
- Web Dashboard 独立成小节
- 新增 GUI event loop 类比、stdout 传送带类比
- 术语统一：架构章节统一「Ink TUI」「经典 TUI」

### 事实审核

- 检查断言数：42
- ✅ 通过：37
- ⚠️ 存疑：3
- ❌ 错误：2
- 二次验证：全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| COMMAND_REGISTRY 行号 | commands.py:45（@dataclass） | :64 |
| Mermaid worktree 行号 | _setup_worktree() 14508 | 14519 + 隔离提示注入 14587 |
| 皮肤字典行号 | skin_engine.py:165-568（568 是 charizard 起始） | 164-645 |
| process_loop warning 行号 | cli.py:14131（except 行） | 14132 |
| 皮肤继承定位 | get_color/get_branding:142-157（调用方 fallback） | _build_skin_config:689-692（真正继承 default） |

---

## 第 11 章：Cron 调度

### 深度审核

- 检查机制数：9
- 🟢 深度足够：2
- 🟡 部分缺失：5
- 🔴 严重不足：2

主要补充（二次验证全部确认，含 1 处草稿错误修正）：
- **修正草稿错误**：repeat.times 耗尽是**直接删除任务**（`jobs.py:888 jobs.pop`），非草稿原写的「completed 态」
- 活性超时（`scheduler.py:1591-1677`，默认 600s 无活动 / `HERMES_CRON_TIMEOUT` / 0=无限，轮询 get_activity_summary 而非掐总时长）
- ContextVars 投递隔离（`scheduler.py:1337-1373`）+ 清空 `HERMES_SESSION_*` 的原因（防 terminal/tts/skills/send_message 工具误判为真实用户消息）——并发安全第二层
- `state=error` 状态（`jobs.py:901-918`，循环任务算不出 next_run 时不静默禁用，#16265）+ `last_delivery_error` 字段
- `.env` 每次热加载（key 改了不用重启）+ `max_turns=90` 来自 config
- workdir 注入 AGENTS.md/CLAUDE.md（不只是设 cwd）+ EMFILE 资源清理（#10200）
- asyncio 冲突时的 ThreadPoolExecutor 回退投递
- ticker 兼做 gateway 周期性维护（频道目录/缓存/Curator）

### 文学性审核

- 改进建议数：9（必须改 3 / 建议改 6）
- 采纳 8 个（设计决策表简短复述保留，判断更利于扫读）

主要改进：
- 补 3 处节间过渡句（jobs.json→调度解析、tick 四机制→并发、单任务→投递锚点）
- gateway 首次出现加说明、ContextVar 加一句释义
- crontab「行不通」三理由压成一句、密集段落拆两段、context_from 补 ID 获取方式

### 事实审核

- 检查断言数：32
- ✅ 通过：27
- ⚠️ 存疑：4
- ❌ 错误：1
- 二次验证：全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| Mermaid get_due_jobs 条件 | next_run<=now 且 state=scheduled | enabled=True 且 next_run<=now（过滤的是 enabled 非 state） |
| disabled_toolsets | 仅 cronjob | cronjob/messaging/clarify 三个（scheduler.py:1577） |
| 脚本超时层数 | 三层 env→config→默认 | 另有测试用 module 级覆盖 |
| job ID 描述 | UUID 前 12 位 | uuid4().hex 前 12 位 |

### 事实审核（2026-06-02 新机制复跑）

- 触发：消除 prompts/agents 冗余后，用新流程（`subagent_type: factual-reviewer`，系统提示锁定）复跑验证
- 检查断言数：47（较首轮 32 更彻底）；✅ 43 / ⚠️ 2 / ❌ 1；二次验证：主线独立读源码，2 个实质发现**全部确认为真**
- **首轮（32 断言）漏掉的 2 处真错误**（本轮新捕获并已修）：
  | 项目 | 修正前 | 修正后 | 源码依据 |
  |------|--------|--------|----------|
  | wake gate `context` 字段 | 「`{"wakeAgent": true, "context": {...}}` 把数据传给 agent」 | 数据靠整段 stdout 注入 prompt；`_parse_wake_gate` 只读 `wakeAgent` 键，`context` 不被解析 | `scheduler.py:931-954`（仅 `gate.get("wakeAgent")`）、977-994（stdout 注入） |
  | `[SILENT]` 检测 | 「以 `[SILENT]` 开头」（L122/L236） | 「回复中**含** `[SILENT]`」，子串匹配 + 不区分大小写 | `scheduler.py:1881`（`SILENT_MARKER in ...strip().upper()`） |
- 未采纳为错误（记录在案）：`context_from` 8K 截断未提（漏报非错误）；2 处图表缺口（no_agent/agent 分叉、三级投递回退）属建议
- **教训**：首轮「全部确认采纳」不等于无错——32 断言的覆盖面不够，这两个错都需要钻进 `_parse_wake_gate` / 比对 `in` vs `startswith` 才看得出，浅层枚举会跳过。详见 99-工作日志同日反省条。

---

## 第 12 章：批量运行与轨迹生成

> 范围变更：原「批量运行与 RL」。v0.14.0 已把 RL 训练环境（environments/、rl_cli.py、tinker-atropos、tool_call_parsers）整个移出主仓，用户拍板重定为「批量运行与轨迹生成」，只讲实际存在的数据生成管线（触发 WORKFLOW §六 红线，已请示）。

### 深度审核

- 检查机制数：7
- 🟢 深度足够：1
- 🟡 部分缺失：4
- 🔴 严重不足：2

主要补充（二次验证全部确认）：
- tokenizer 依赖（`trajectory_compressor.py:86` = `moonshotai/Kimi-K2-Thinking`，token 数按它算有模型偏差；失败 RuntimeError + `len//4` 粗回退）
- 批级并行而非 prompt 级（`batch_runner.py:442` 同 batch 内顺序）——`num_workers=4,batch_size=10` 同时只跑 4 个 Agent 非 40
- 按需最小压缩算法（累积到省够就停，非全压中间）+ 中线 n//2 切分导致短轨迹压不动
- 推理过滤双条件 + 被丢弃轨迹计入 completed 不重跑（`:459`）
- 合并阶段幻觉工具过滤（`:1028`）+ skip_context_files/skip_memory 数据清洁（`:344`）
- mini_swe 不用 AIAgent、单 terminal 工具、`MINI_SWE_AGENT_FINAL_OUTPUT` echo 哨兵收尾
- 轨迹归一化四点（推理统一 think / 多 tool 响应合一条 / base64→text_summary / prefill 不入轨迹）

### 文学性审核

- 改进建议数：9（必须改 3 / 建议改 6）
- 全部采纳

主要改进：
- 使用指南→架构过渡句、四设计俯瞰总结句（仿 11 章）
- ShareGPT 首次出现加释义、echo 哨兵加背景、mini_swe 回归数据工厂比喻
- 拆密集段（推理过滤排错陷阱单列、token 段注意项单列）、硬币类比补 2ᴺ

### 事实审核

- 检查断言数：32
- ✅ 通过：27
- ⚠️ 存疑：4
- ❌ 错误：1
- 二次验证：全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| MINI_SWE 哨兵行号 | mini_swe_runner.py:96 | :97（定义）+ :535（检测） |
| 镜像校验范围 | 每个 prompt 前都验证 | 仅 TERMINAL_ENV=docker，Modal/local 跳过 |
| DISTRIBUTIONS 行号 | :31-220 | :29-220（29 是赋值行） |
| 默认模型 | claude-sonnet-4.6 | 补注：CLI 默认 sonnet，类默认 opus |
| [CONTEXT SUMMARY] 角色 | 未说明 | from="human"（:808） |

---

## 第 13 章：工程实践

### 深度审核

- 检查机制数：8
- 🟢 深度足够：3
- 🟡 部分缺失：4
- 🔴 严重不足：1

主要补充（二次验证全部确认，含拒绝 1 处误报）：
- **拒绝误报**：审核 Agent 质疑「6 路 CI 矩阵」，二次读 `tests.yml:30` 确认 `slice: [1..6]` 真实，保留
- `atomic_roundtrip_yaml_update`（`utils.py:191`）：config.yaml 用保注释的 roundtrip，非会清注释的 atomic_yaml_write（原子写三函数三场景）
- `isolation_level=None`（`hermes_state.py:352`）：BEGIN IMMEDIATE 正确工作的前提
- `_last_init_error`（`:66`）：NFS 初始化失败→斜杠命令诊断提示的链路
- 声明式列协调约束：NOT NULL 无默认值加不了列、依赖待补列的索引须在 reconcile 后建（`:580`）；_parse 用内存 SQLite 避正则边界
- per-token CJK 路由（`:2236`，#20494）：「广西 OR 桂林」每词 2 字触发 LIKE 降级
- `_live_system_guard`（`conftest.py:523`）：拦 os.kill/subprocess 防测试 SIGTERM 掉开发者的 gateway
- `COMPONENT_PREFIXES` 双角色 + 懒安装 `allow_lazy_installs:false`

### 文学性审核

- 改进建议数：10（必须改 3 / 建议改 7）
- 全部采纳

主要改进：
- 边界说明移出导言（不打断收尾期待）、三处节间过渡
- convoy→「车队效应」中文术语前置 + 比喻延展、roundtrip/hermetic 加释义、密集 schema 段拆分
- **全系列结语收尾重写**（地基比喻收束 + 末句从事务性「完成走读」改为「这套源码的故事讲完了」，给终章余韵）

### 事实审核

- 检查断言数：28
- ✅ 通过：24
- ⚠️ 存疑：3
- ❌ 错误：1
- 二次验证：全部确认采纳

主要修正：
| 项目 | 修正前 | 修正后 |
|------|--------|--------|
| 安全赏金措辞 | 「不在安全赏金范围内」 | Hermes 无 bug bounty；prompt injection 等「不在漏洞受理范围内」（§3.2） |
| 测试文件计数 | 1,208 个测试文件 | 1,208 个 .py（test_*.py 1,167 个，余为夹具） |
| _live_system_guard 行号 | conftest.py:522 | :523（def 行） |
| 整进程包装边界 | 容器/沙箱（泛指） | Hermes Docker image / NVIDIA OpenShell（点名） |

---

## 终审完整性审核（全 14 章）+ 两处遗漏补齐

### 完整性审核（五项检查）

文档完整性优秀，无子系统遗漏、无严重矛盾。真问题：
- 第 12 章重定名「批量运行与 RL」→「批量运行与轨迹生成」时，00/10/11 章旧称交叉引用未同步——已改
- 09 章失效链接 `guides/multi-agent`（不存在）→ `guides/delegation-patterns`——已改
- **两处真遗漏**（CLAUDE.md 承诺 02 章含 LSP 专题但 0 提及；语音模式真实存在仅零星提及）——已补

### 补 LSP 专题（02 章）+ 语音模式（10 章）

- 事实+深度审核：22 断言，15✅ 4⚠️ 2❌
- 文学性审核：7 建议（3 必改），全采纳
- 二次验证修正：reporter 行号（16/18）、ServerDef 数（25）、LSP 仅本地后端激活、LSP 仅语法通过时触发、STT 回退含 xai、流式 TTS 仅 ElevenLabs、TTS 近十种 provider

### 自我纠错记录

终审期间因终端输出故障，一度（commit a4c3e57）误把 LSP 真遗漏判为「幻觉/目录不存在」并提交，随后用 `ls agent/lsp/` 核实更正（commit 1d2efc7）。教训：终端输出可能损坏，硬数字须用稳定法（写文件再读 / Read 工具）核验，故障期间不下结论。

---

## 2026-06-02 批量事实复审（新机制，02/03/05/08 四章）

- **缘起**：11 章复审在「已定稿」文档里查出 2 个首轮漏掉的真错误后，对机制密集的 02/03/05/08 四章用新流程（`subagent_type: factual-reviewer`，系统提示锁定）复跑，每条 ❌/⚠️ 经主线独立 grep 二次验证。
- **二次验证确认并已修的错误**：

| 章 | 错误 | 修正 | 源码依据（主线独立核） |
|----|------|------|----------------------|
| 02 | LSP「25 种语言服务器」 | → **26** | `agent/lsp/servers.py` SERVERS 列表（831-1015）含 26 个 `ServerDef(`；`class ServerDef:` 在 127 行不计入 |
| 02 | 凭证池创建「`hermes_cli/auth.py`」 | → `runtime_provider.py` 调 `load_pool` 注入；auth.py 仅存取数据 | runtime_provider.py:421/456/471 |
| 02 | 写文件后调 `get_diagnostics_sync` | → `_maybe_lsp_diagnostics` | `tools/file_operations.py:1443`；全文件无 `get_diagnostics_sync` |
| 03 | DANGEROUS_PATTERNS「15 个」 | → **52** | `approval.py:316` 起列表实测 52 条（代码自注释写 47，均远超 15） |
| 03 | MCP 配置键 `mcp.servers`（嵌套，含 YAML 示例） | → `mcp_servers`（顶层平铺） | `tools/mcp_tool.py:9/15`、官方 security.md:463 |
| 03 | `post_tool_call`:825 / `transform_tool_result`:866 | → **851 / 872**（825/866 是注释行） | 根目录 `model_tools.py` invoke_hook 实际行 |
| 03 | 结果治理「`_invoke_tool()`（run_agent.py）」 | → `agent/tool_executor.py` | tool_executor.py:422/845 调 `maybe_persist_tool_result` |
| 03 | `_PATH_SCOPED_TOOLS`={write_file,patch} | → 补 `read_file`（实为三个） | tool_dispatch_helpers.py:59 |
| 03 | computer_use「5 个文件」 | → 6 个 .py | `ls tools/computer_use/*.py` = 6 |
| 05 | busy_input_mode 含 `ignore`（4 种） | → 删 ignore，仅 interrupt/queue/steer | `_load_busy_input_mode` run.py:2821-2831 只返回这三个 |
| 05 | 扩展点「3 个方法就能跑」 | → **4 个**（补 get_chat_info），与本章表格对齐 | gateway/platforms/base.py 内 4 个 @abstractmethod |
| 05 | `[SILENT]`「以…开头」 | → 子串匹配（同 11 章） | scheduler.py:1881 |
| 08 | model-providers「30 个子目录/Provider」 | → 29 个子目录、30 余个 Provider | `ls -d */` = 29；register_provider 调用 34 次 |
| 08 | context_engine 目录有「LCM 引擎实现」 | → 目录仅 `__init__.py`，LCM 只是 docstring 示例 | `ls plugins/context_engine/` |
| 08 | 平台插件用 `pip_dependencies` 字段 | → 平台用 `requires_env`；`pip_dependencies` 是记忆插件用 | 平台 plugin.yaml 0 个含该字段、记忆 6 个 |
| 08 | Langfuse「2 个钩子」 | → **6 个**（pre/post × api_request/llm_call/tool_call） | langfuse/__init__.py register_hook |

- **⭐ 二次验证拦下的审核 Agent 反向误判（未改文档）**：05 章审核称代码树「另 24 个平台文件」应为 22（它数成列出 9 个文件）。主线核实：树只列了 7 个平台文件，`platforms/` 共 31 个 .py，31−7=**24，文档原本正确**。审核 Agent 自己数错——这正是「审核发现必须二次验证」拦截反向错误的样本。
- **未改（判断为可接受）**：02 章 OAuth 刷新失败处理的简化描述；08 章 memory「11,647 行」（= 8 个 provider 子目录之和，内部自洽，未含顶层 `__init__.py` 407 行）；computer_use 计数已按含 `__init__.py` 口径改为 6。
- **附带发现**：step 25.1 曾把 ServerDef「修正」为 25，实为 26——上次二次验证也少数一个，再次说明计数须用稳定法（本次用 Python 在 SERVERS 列表范围内精确计数）。

## 2026-06-26 批量事实复审（新机制，00/01/04 三章）

- **缘起**：续 02/03/05/08 四章复审，对其余章节继续用新流程（`subagent_type: factual-reviewer`，系统提示锁定，含「挖隐性断言」要求）复跑。第一批 00/01/04 并行起三个审核 Agent，每条 ❌/⚠️ 经主线独立 grep 二次验证（计数/行号类一律写文件再读，规避终端输出损坏）。本批确认 **17 个真错误**、**拦下 4 处审核 Agent 反向误报**。
- **二次验证确认并已修的错误**：

| 章 | 错误 | 修正 | 源码依据（主线独立核） |
|----|------|------|----------------------|
| 00 | `hermes_cli/` 行数 101,706 | → **100,759** | `find hermes_cli -name '*.py' -not -path '*__pycache__*' \| xargs wc -l`（00/01 两 Agent 独立同得） |
| 00 | provider「30+」挂在行号 `cli-config.yaml.example:13-43` | 行号引用改写：13-43 注释段实列 22 个；30+ 据 `CANONICAL_PROVIDERS`（静态 35 条） | `sed -n '13,43p'` 数得 22；models.py:924 列表 Python 数得 35 个 `ProviderEntry(` |
| 00 | 「有副作用的工具串行执行」 | → 文件读写在目标路径不重叠时也并行，重叠才串行 | `run_agent.py:3967-3969` docstring「file reads/writes may do so only when their target paths do not overlap」 |
| 00 | 系统提示「会话内只构建一次」 | → 补「上下文压缩会触发重建」 | `agent/system_prompt.py:3-4`「only context compression triggers a rebuild」 |
| 00 | 「主线程以 100ms 轮询队列」 | → 后台 daemon 线程 `process_loop` 轮询；主线程维护 REPL UI | `cli.py:14135` `process_thread = threading.Thread(target=process_loop, daemon=True)`，轮询 `_pending_input.get(timeout=0.1)` 在该线程内 |
| 01 | kanban 认领「UUID + TTL」 | → `host:pid` 标识 + TTL | `hermes_cli/kanban_db.py:1504-1511` `_claimer_id()` 返回 `f"{host}:{os.getpid()}"` |
| 01 | setup 向导「五个步骤」且漏 TTS、顺序错 | → **六步**（SETUP_SECTIONS：model/tts/terminal/gateway/tools/agent），与使用指南对齐 | `setup.py` SETUP_SECTIONS 6 项 |
| 01 | 插件白名单「只有显式列出才加载」 | → 内置 backend/platform 自动加载，其余才受白名单 | `hermes_cli/plugins.py:1034-1044` `if manifest.source=="bundled" and kind in {"backend","platform"}: _load_plugin; continue` |
| 01 | 安装系统服务「`gateway.py:5006`」 | → 5006 是 `gateway_command` 分发器；实际 `systemd_install`:2440 / `launchd_install`:2900 | `grep def systemd_install/launchd_install` = 2440/2900 |
| 01 | hermes_cli「约 101,700 行」 | → 约 100,800（实测 100,759） | 同 00 行数 |
| 01 | resolve_alias 第三级「模糊匹配」+ 漏内置家族别名 | → 前缀 `startswith`（非编辑距离），补 `MODEL_ALIASES` 一级 | `model_switch.py:481/509` `MODEL_ALIASES.get` + `mid.lower().startswith(prefix)` |
| 04 | patch 动作「unified diff 格式」 | → `old_string`/`new_string` 定向查找替换（复用 fuzzy_match 引擎） | `skill_manager_tool.py:566-614` `_patch_skill` 签名仅 old/new_string，无 diff 参数 |
| 04 | polymarket 示例 tags 三个 | → 四个（补 `trading`） | `skills/research/polymarket/SKILL.md:6` |
| 04 | 后台审查「stdout 重定向 devnull」 | → stdout+stderr 都重定向；warning 在 context 外靠 logger 落盘 | `background_review.py:361-363` `redirect_stdout`+`redirect_stderr` |
| 04 | 「skill_view 成功 bump view_count」 | → 同时 bump `use_count`（主动加载即视为使用，刷新 last_used_at） | `skills_tool.py:1549-1554` `bump_view` + `bump_use` |
| 04 | Curator 触发条件「curator.py:56-59」 | 行号改写：56-59 是 `DEFAULT_*` 常量；触发判定 `should_run_now`:199 / `maybe_run_curator`:1763 | grep def 实测 |
| 04 | 代码组织漏 `tools/skills_hub.py` | → 补一行（3,456 行，含 `OptionalSkillSource`，optional-skills 经此暴露） | `wc -l` 3456；`OptionalSkillSource` 在 2535 |

- **⭐ 二次验证拦下的审核 Agent 反向误报（未改文档）——00 章 git 统计 4 处**：审核 Agent 用 HEAD-only + commit-date + 去重 commit 的口径，标 288/26/2514/4008 为错；主线核实 **HEAD 恰为文档基准 commit `3bace071b`（无漂移）**，并复原文档作者口径为 **`git log --all` + author-date + 数署名行**：
  - Claude 共创「288」= `git log --all \| grep -ic 'co-authored-by.*claude'` 原始署名行 = **288**（279 唯一 commit，9 个 commit 多条署名）✓ 文档对
  - Hermes 共创「26」= `--all` 含 hermes 的 co-author 唯一 commit = **26** ✓ 文档对（审核只数 HEAD 得 23）
  - 3 月「2514」/ 4 月「4008」= author-date `%ad` 按月过滤 = **2514 / 4008** ✓ 文档对（审核用 commit-date `--since/--until` 得 2458/3855）
  - 结论：四数全部可复现、口径自洽，**全是审核 Agent 误报**。这是「审核发现必须双向二次验证」最强的一次样本——若盲采审核意见会把 4 个正确数字改错。
- **教训**：① 凡 git 统计先固定口径（HEAD vs `--all`、author-date vs commit-date、署名行 vs 去重 commit），同一口径下复现才算数；② 行号引用类（5006/56-59）即便不算「错」也常指向分发器/常量而非真实逻辑，复审一律落到真实实现行；③ 隐性能力/语义断言（patch 格式、stdout 范围、skill_view 副作用、并行条件、系统提示重建）仍是首轮漏报重灾区，与 02/03/05/08 同病。

### 批次2（06/07/09）

- **结果**：三章并行审核，**确认 19 个真错误、拦下 1 处误报**。
- **二次验证确认并已修的错误**：

| 章 | 错误 | 修正 | 源码依据（主线独立核） |
|----|------|------|----------------------|
| 06 | patch 不触发审批的模式「以 append 为例」 | → append 非合法 mode；不触发的是 `mode="patch"`(V4A) | `edit_approval.py:130-137` build_edit_proposal 只为 write_file/patch-replace 生成提案；file_tools.py:911/970 仅 replace/patch 两 mode |
| 06 | 60s 超时行号 `:234` | → :238（timeout=60.0 所在行） | `edit_approval.py:238` |
| 06 | accept_edits「自动接受文件编辑」/ dont_ask「跳过所有审批」 | → accept_edits 仅 workspace/tmp；dont_ask 敏感路径仍询问 | `edit_approval.py:156-178`；server.py:549-558 SessionMode 描述 |
| 06 | QUEUE_LIMIT 行号 `:329` | → :191(定义)，:328(裁剪) | `mcp_serve.py:191/328` |
| 06 | permissions_respond 行号 `:304` | → :840(def) | `mcp_serve.py:840` |
| 06 | read_file 格式化「IDE 不需看完整内容」 | → 完整内容仍传，fenced 是防 Zed 把 `\|` 解析成表格 | `acp_adapter/tools.py:288-311` _format_read_file_result |
| 06 | 头部「10 个 .py」vs 代码树列 9（漏 `__init__.py`） | 代码树补 `__init__.py` | `ls acp_adapter/*.py`=10 |
| 07 | inject_message return False 行号 `:371` | → :373 | `hermes_cli/plugins.py:373` |
| 07 | pre_tool_call「后续插件不再检查」 | → invoke_hook 调用所有回调，再取第一个 block 消息 | `plugins.py:1413` invoke_hook 无早退；get_pre_tool_call_block_message 取首个 |
| 07 | register_cli_command「以 hermes spotify 为例」 | → spotify 只 register_tool；真实例 `hermes meet`(google_meet)/`hermes teams-pipeline` | spotify/__init__.py:59 只 register_tool；google_meet:92 name="meet"；teams_pipeline:13 name="teams-pipeline" |
| 07 | pre/post_approval_request 参数混写 `choice` | → choice 仅 post_approval_response 有 | `plugins.py:160-167` 注释 |
| 07 | 钩子图未画 subagent_stop/pre_gateway_dispatch | 图注补说明（合计 17 种 VALID_HOOKS） | VALID_HOOKS 17 项已验 |
| 09 | 「kanban_complete()（kanban_db.py:2854）」函数名 | → `complete_task()`（kanban_complete 是 kanban_tools.py 工具名） | kanban_db.py:2854 `def complete_task`；全文件无 def kanban_complete |
| 09 | board current 路径 `~/.hermes/kanban/kanban/current`（双 kanban） | → `~/.hermes/kanban/current` | `current_board_path`=kanban_home()/"kanban"/"current"；kanban_home()→`<root>`=~/.hermes |
| 09 | recompute_ready「父任务全部 done 才提升」(4 处) | → done **或 archived** | kanban_db.py:2258-2264 `in ("done","archived")` |
| 09 | complete_task 步骤2「DB事务…重置 consecutive_failures」 | → 重置(_clear_failure_counter:3028)在主事务外、散文扫描后，单列步骤4 | kanban_db.py:3028 在 write_txn 块外 |
| 09 | kanban_show Worker「✅（仅自己的任务）」 | → 任意任务（只读无 ownership 限制） | `_handle_show`(255) 不调 `_enforce_worker_task_ownership`；仅 complete/block/heartbeat/unblock 调 |
| 09 | Task「30+ 字段」 | → 恰好 30 | kanban_db.py:603-731 字段行计 30 |
| 09 | env 注入范围 `:5607-5641` | → 5600 起（HERMES_HOME） | kanban_db.py:5600 `env["HERMES_HOME"]` |

- **⭐ 二次验证拦下的误报（未改文档）**：06 章审核称「72 工具」源码无硬编码、不可验。主线 `grep -rn "registry.register(" tools/*.py | grep -v ...` 实测 = **72**，文档对（与 00 章同一计数已两度验证）。审核 Agent 用 `_HERMES_CORE_TOOLS`(48) 这条错路子去数。
- **教训**：① 行号引用密集章节（06/07）首轮普遍指向「def 行/常量定义行/使用处」而非语义所在行，复审逐个落到真实行；② 多模式/多状态枚举最易漏一项（patch mode 漏 V4A、父任务条件漏 archived、choice 字段归属、accept_edits/dont_ask 边界）——凡「A 触发 B」「仅 X 才」类断言必须回源码数全分支；③ 工具名 vs DB 函数名混用（kanban_complete vs complete_task）是跨层命名的典型陷阱。

### 批次3（10/12/13）★ 全 14 章复审收官

- **结果**：三章共 **7 个真错误**修正。**10 章质量极高**（89 断言 87✅ 0❌，仅 2 处行号小瑕），12 章 1 个真错 + 1 改进，13 章 3 处归因/泛化校正。
- **二次验证确认并已修的错误**：

| 章 | 错误 | 修正 | 源码依据（主线独立核） |
|----|------|------|----------------------|
| 10 | spinner_loop「第三个 daemon 线程（cli.py:13999）」 | 补 def 行 13983（13999 是建线程行） | cli.py:13983 def / 13999 Thread() |
| 10 | web token「每次启动生成（web_server.py:125）」 | 生成在 :87（`secrets.token_urlsafe(32)`），:125 是验证函数 | web_server.py:87/125/146 |
| 12 | `--model` 默认值表格 `claude-sonnet-4.6` | → `anthropic/claude-sonnet-4.6`（缺前缀，与同文档 bash 示例自相矛盾） | batch_runner.py:1152 |
| 12 | research 分布「web 90%+browser 70%」 | 补「另含 vision 50/moa 40/terminal 10」（实为 5 个工具集） | toolset_distributions.py:57-65 |
| 13 | 「SQLite 默认锁等待 30 秒」 | → 「落在 SQLite 内置 busy handler 干等最长 30 秒」（贴代码注释措辞，去「默认」误归因） | hermes_state.py:344 注释「busy handler for up to 30s」，timeout=1.0 |
| 13 | jobs.json/OAuth/active_profile「全靠 atomic_json_write/atomic_yaml_write 写」 | → Cron/OAuth 走底层 `atomic_replace`；checkpoint 走 atomic_json_write；config 走 atomic_roundtrip_yaml_update | cron/jobs.py:25/442、auth.py:1054/1944 用 atomic_replace |
| 13 | 「v13 起改用声明式列协调」 | → 「现行（SCHEMA_VERSION=13）采用」（代码无引入版本记录，去「起」推断） | hermes_state.py:555 注释只述模式不述引入版本 |

- **本批无明显反向误报**（10 章两处属行号精度、非幻觉；12/13 均真错或合理改进）。
- **教训**：① 即便整章近乎零错（10 章 87/89），行号也常落在「建线程行/验证函数行」而非定义行，复审仍有收；② 文档把代码注释里的「up to 30s（busy handler）」转写成「SQLite 默认 30 秒」是典型的"二手归因失真"——凡转述机制成因必回贴原注释；③ 「全靠 X 写」式总括最易过度泛化，落到每个调用点会发现走的是不同函数（atomic_replace vs 各封装）。

---

## ★ 复审里程碑：全 14 章新机制事实复审 100% 完成（2026-06-26）

- **覆盖**：02/03/05/08/11（前序）+ 00/01/04（批次1）+ 06/07/09（批次2）+ 10/12/13（批次3）= **14/14 章**。
- **总账**：本三批（00-13 中的 9 章）共 **43 个真错误**修正（批次1:17、批次2:19、批次3:7）+ **5 处审核 Agent 反向误报**经二次验证拦下（00 章 4 个 git 统计口径 + 06 章 72 工具计数）；连同前序 5 章的 18 个真错，复审累计捕获 **61 个首轮漏掉的真错误**。
- **结论**：用户「方法变了、过去成果是否可信」的质疑被坐实——首轮事实审核存在系统性的「隐性断言枚举不全」漏报。新机制（系统提示锁定 + 强制双向二次验证 + 可复跑）是有效闭环。14 章现已全部经加强版复审，可进入英文翻译阶段。

---

## v0.18.2 版本更新审核（2026-07-09 起）

> 基准：tag `v2026.7.7.2`（commit `9de9c25f6`）。重章 depth+factual 双审，中/轻章 factual 认证审；每条 ⚠️/❌ 主线双向二次验证。

### 00 章（项目全景）— factual 认证审

- **审核 Agent**：`subagent_type: factual-reviewer`（升级版手册：5 条验证方法硬性要求已生效）
- **统计**：~95 断言，82✅ / 6⚠️ / 4❌（其中 1 条 ❌ 为跨章一致性预警，实为 03 章待更新）
- **二次验证确认并已修**：

| 类别 | 错误 | 修正 | 源码依据（主线独立核） |
|------|------|------|----------------------|
| ❌ | 核心依赖「31 个，精确锁定不使用范围」 | → 30 个（tomllib 解析）；5 个基础设施包（urllib3/fastapi/uvicorn 等）实用范围，补注"注释与现实轻微漂移" | pyproject.toml `[project.dependencies]` len=30；非 `==` 共 5 条 |
| ❌ | 配置优先级「CLI > 环境变量 > yaml > 默认」 | → **CLI > config.yaml > .env > 默认**（非密钥两边都设时 yaml 赢；密钥必须进 .env） | website/docs/user-guide/configuration.md:49-60 "Configuration Precedence" |
| ❌ | 「只读工具（以 read_file 为例）总是并行」 | → read_file 在 `_PATH_SCOPED_TOOLS`，走路径重叠检查（其 `_PARALLEL_SAFE_TOOLS` 成员资格是死路径）；无条件并行例子改 web_search/search_files/session_search；补三档判定描述 + 锚点 tool_dispatch_helpers.py:104 | `_should_parallelize_tool_batch` 中 `_PATH_SCOPED_TOOLS` 分支在前且 `continue` |
| ⚠️ | 「以 OpenRouter 为例，一个 Key 访问 300+ 模型」 | 仓内"300+"唯一出处是 Nous Portal 注册表条目 → 300+ 归 Nous Portal（标"注册表自述"），OpenRouter 例去掉具体数字 | hermes_cli/models.py:1031 ProviderEntry("nous"...300+ models) |
| ⚠️ | 「32 个斜杠命令 handler 拆进 CLICommandsMixin」现在时态 | → 拆分当时 32 个，现已 40 个（新命令直接进 mixin）；顺带补 god-file 拆分示意图 | grep -c "def _handle_.*command" = 40；commit 0904bc7ea 标题 32 |
| ⚠️ | 「69 处标记 Hermes Agent 自身为共同作者」口径偏宽 | → 69 处 Hermes 系身份（52 条明确 "Hermes Agent"，余为 subagent 等变体） | git log --all %B 逐行计：总 69，Hermes Agent 52 |
| ⚠️ | 「贡献者 1,934 位」口径未标 | → 标注口径「原始署名去重，同一人多署名重复计」（mailmap 口径为 1,927） | %an 去重=1,934；%aN 去重=1,927 |

- **⚠️ 跨章预警（本章不改）**：03 章「72 工具/96 工具文件」与新基准不符（实测 69/112），留待 03 章更新时修。
- **审核确认的高危正确项**（差点被当错）：Teknium 合并署名 7,356（审核 Agent 深挖 4 种大小写变体 + 8 条 Hermes Agent 署名/teknium 邮箱后精确复现）；"god-file decomposition campaign" 是源码 docstring 原文非文档自造；69 工具需复刻 registry 的"仅模块级调用"判定才能复现（naive grep 得 77-78）。
- **未采纳**：压缩器"失败模式"、Agent 缓存"可配置性"等 9 问补深建议——属 02/05 章深度范围，00 章保持全景颗粒度；并行判定决策树图——已用三档文字描述覆盖，避免图表膨胀。

### 01 章（基础设施层）— 重章双审（depth + factual）

- **深度审核**（depth-reviewer）：11 机制体检，3🟢 / 4🟡 / 4🔴。主线逐项独立核源码后全部采纳：
  - 🔴 补 `resolve_provider()` auto 8 级优先级链（auth.py:1619-1628 docstring）+ #29285（导出 Key 抢占 OAuth 并打 warning）+ #42130（pool 可见性）+ copilot/lmstudio 刻意排除
  - 🔴 补 gateway 运行时状态收敛 5 机制（SIGUSR1 优雅 drain :248 / 孤儿回收 :1458 / PID 不匹配 :1339 / start-limit 恢复 :1147 / unit 漂移 :2828）
  - 🔴 排错表补 `hermes doctor` 入口（check_ok/warn/fail :177-183）
  - 🔴 补 OAuth 排障（HERMES_OAUTH_TRACE :861、设备码轮询超时 :4584、Profile 全局 auth 只读回退 :901）
  - 🟡 runtime 链补 `_resolve_explicit_runtime`（:1354/:1658）+ pool 三门槛（:1682-1686）并改图；Profile 补粘性文件/S6 例外/sudo 回退/三路错误处理/multiplex（profiles_to_serve :949）并改图；callbacks 补三回调超时表（120/120/60s，超时行为各异）+ 审批锁；auth 锁补内核 flock 语义 + 15s 超时（:72）
- **事实审核**（factual-reviewer）：~70 断言，58✅ / 6⚠️ / 3❌。二次验证账：

| 类别 | 错误 | 修正 | 源码依据（主线独立核） |
|------|------|------|----------------------|
| ❌ | 配置示例 `security.require_approval` | 全仓零匹配，键不存在 → 改真实的 `approvals.mode/timeout`（config.py:2459） | grep 零命中；approvals 块实测 |
| ❌ | `--clone-all`「会话也带走」 | → 会话史显式排除（state.db/sessions/backups/快照，profiles.py:118-126），记忆确实带走 | `_CLONE_ALL_HISTORY_EXCLUDE_ROOT` 注释明说防几十 GB 膨胀 |
| ❌ | god-file/managed_scope「v0.18 新增」 | → **v0.17.0**（commits 2026-06-08，首含 tag v2026.6.19=0.17.0）；同步修 00 章同源错误 + 备忘新增"版本归属速查"表 | git cat-file：v0.16 无 / v0.17 有；git tag --contains |
| ⚠️半拦 | 「main.py v0.14 时 13,847 行」被审核标不符（其测 12,408） | **数字不改**——审核用 tag v2026.5.16 快照，本文档基准是 commit 3bace071b，实测 13,847 精确成立；但"第二大"确错 → 改"第三大"（gateway/run.py 18,318 > cli.py 14,785 > main.py） | git show 3bace071b:hermes_cli/main.py \| wc -l = 13847 |
| ⚠️ | 「任何模块 import 之前」字面不准 | → "任何读取 HERMES_HOME 的模块 import 之前"（subcommands 解析器先 import 但不碰 HERMES_HOME；调用点 :513） | main.py:268-303 import 先于 :513 |
| ⚠️ | fast TUI「跳过 Python 子命令解析」 | → 跳过的是完整子命令树构建，轻量顶层解析器仍跑（docstring 原话） | main.py:12508-12530 |
| ⚠️ | 「约 1,440 行可调参数」口径不明 | → "近 500 个可调配置项"（AST 数 DEFAULT_CONFIG 叶子 = 497） | ast 脚本实测 |
| ⚠️ | 「40 个 build_*_parser 文件」 | → 38 个定义 parser + __init__/_shared 共 40 | grep -l 计数 |
| ⚠️ | HERMES_OAUTH_TRACE `auth.py:860` | → :861（860 是 def 行） | 语义行原则 |

- **审核确认的高危正确项**：`_resolve_named_custom_runtime` 引用 :1606 是刻意的调用点引用非笔误；#29285 warning 叙述逐句对应源码注释；`--clone` 四项复制清单逐字与 --help 一致。
- **重要教训**：**跨章一致不等于正确**——00/01 都写"v0.18"是同源推断错误（"新基准 0.18.2 ⇒ 新东西都是 v0.18"），版本归属必须 `git tag --contains` 实测。已写入备忘版本归属速查表。

### 02 章（Agent 核心）— 重章双审（depth + factual）

- **深度审核**（depth-reviewer）：15 机制体检，6🟢 / 5🟡 / 4🔴。主线逐项独立核源码后全部采纳：
  - 🔴 **`_budget_grace_call` 是无写入点的残留代码**（全仓只有 init False + 消费两处）——文档原按它解释"预算耗尽宽限调用"会让读者扑空。真机制：turn_finalizer.py:53-70 → handle_max_iterations（chat_completion_helpers.py:1565，**剥离工具**的单次总结调用）+ kanban 场景代模型上报 timed_out 计入熔断（#29747）。已重写 + 补 refund() 退款机制
  - 🔴 压缩失败闭环全缺 → 补 compress() 五步算法（docstring :2710-2715）+ 三分支降级图（认证/网络无条件 abort 不丢消息 #29559；abort_on_summary_failure；默认静态兜底丢中间消息）+ 排错表两行
  - 🔴 重试真实分支 → 补 max_retries 默认 3（#11616）、FailoverReason 四分叉（billing 立即轮转/rate_limit 两级/auth entitlement 检测+刷新上限 #26080/upstream_rate_limit 不碰池）、pool_may_recover 提前 fallback 判断、专项自动修复清单、Nous 跨会话熔断、Z.AI 阶梯退避
  - 🔴 MoA 双路径被合并描述 → 拆两条：标记编码路径（aggregate_moa_context 合成意见交回原模型）vs 虚拟 Provider 路径（MoAClient 拦截每次调用、aggregator 代行工具调用）；补 fanout per_iteration/user_turn 成本开关、advisor 系统提示、工具结果 4000 字符裁剪
  - 🟡 凭证冷却三档（401→5min/429→1h/默认 1h + reset_at 覆盖）；子 Agent 汇总溢出保护（#9126 动态字符预算+落盘）与 child_timeout 默认无超时+心跳兜底；MoA 注入相对 ❼❽❾ 的位置
- **事实审核**（factual-reviewer）：~100 断言，95✅ / 2⚠️ / 1❌。二次验证账：

| 类别 | 错误 | 修正 | 源码依据（主线独立核） |
|------|------|------|----------------------|
| ❌ | Model Metadata 链"硬编码表→本地服务器兜底→256K"顺序颠倒 + models.dev 归错到第 6 步 | 照抄 docstring :1895-1916 重写全链（本地服务器在硬编码**之前**再查一次 :2271；models.dev 是 5f）——此错自 v1 一路继承 | get_model_context_length docstring + 函数体注释 #7/#8/#9 |
| ⚠️ | `_resolve_auto` 标 :4510 | → 定义 :4104、调用点 :4510 分开标 | grep def 行 |
| ⚠️ | 69 vs 03 章 72 工具 | 本章 69 经审核 Agent 独立 venv 实跑注册表确认精确；03 章欠账已登记，下一章即修 | discover_builtin_tools 实测 |

- **审核确认的高危正确项**：401→5min/429→1h 冷却没搞反；90+3×50=240 三常量逐个成立；"the aggregator is the acting model" 引语逐字命中 MoAChatCompletions docstring。
- **教训**：① 排错入口指向的符号必须验证"有活的写入路径"——`_budget_grace_call` 在循环条件里出现不等于机制存在；② 长 fallback 链靠纯文本转写极易错序，照抄源码 docstring 并标注锚点行最稳。

### 03 章（工具系统）— factual 认证审

- **统计**：~85 断言，72✅ / 5⚠️ / 3❌（24 项行数/文件数全部精确命中）。二次验证账：

| 类别 | 错误 | 修正 | 源码依据（主线独立核） |
|------|------|------|----------------------|
| ❌ | 执行路径图"治理→transform"顺序反（正文对图错） | 图改 POST→TRANSFORM→SIZE→BACK（transform 在 handle_function_call 内 :1309-1338 返回前；持久化在 tool_executor.py:912 返回后） | maybe_persist 调用点 + return result 行 |
| ❌ | threat_patterns"三消费方"含 tool_dispatch_helpers | 实际 import 方：prompt_builder/memory_tool/**cronjob_tools**；定界系统自成一体未接——**模块 docstring 与实际引用脱节，照抄 docstring 也会错** | grep import 全量 |
| ❌ | "AST 失败静默跳过并记 warning" | AST 语法失败**完全静默**（:50-51 return False 无日志）；warning 属 import 阶段失败（:74）——两条失败路径混为一谈 | registry.py 异常分支 |
| ⚠️ | Computer Use "基于 macOS" | → 三平台（check_fn :899 明确 darwin/win32/linux；仓内旧注释仍写 macOS 是陷阱源） | check_computer_use_requirements |
| ⚠️ | tool_search "enabled: false 开启" | → 默认档 **auto**（不配置时 schema 占用超上下文 10% 自动激活，:78-83） | ToolSearchConfig.from_raw |
| ⚠️ | "v0.18 新增 killall 系列" | killall v0.14 基准已有；PowerShell 才是新增（旧版 grep=0/新版=4）；总数 52→73（AST 计数） | git show 3bace071b 对照 |

- **⭐ 审核拦下的口径陷阱**：approval.py "v0.14 时 1,424 行"被质疑（release commit a91a57fa5 是 1,392）——审核 Agent 自己查了本项目 PROGRESS 确认基准是 3bace071b，实测 1,424 精确成立。**审核 Agent 首次主动对齐了项目基准口径**。
- **审核确认的高危正确项**：read_file "在安全名单但路径检查在前"的死路径语义（无行号支撑的纯语义断言，逐行验证成立）；DANGEROUS 73/HARDLINE 12（AST 计数，HARDLINE 两版本间未变——scout 报的"5"是误报）。

### 04 章（技能系统）— factual 认证审

- **统计**：~75 断言，60✅ / 6⚠️ / 8❌。二次验证账（全部主线独立核后修正）：

| 类别 | 错误 | 修正 | 源码依据（主线独立核） |
|------|------|------|----------------------|
| ❌×5 | 版本归属系统性误标：compact_categories×2/删除安全检查/Curator consolidate 拆分标 v0.18；prune_builtins 标 v0.18 | → 前四项 **v0.17**（首含 v2026.6.19）、prune_builtins **v0.16**（首含 v2026.6.5）；payments 分类 → v0.17。用 merge-base --is-ancestor 逐 tag 实测（git tag --contains 会被 backup tag 污染 head -1） | 六 commit 逐一验 |
| ❌ | Skills Hub"对接 agentskills.io" | **整节重写**：实为十数据源聚合器（skills.sh/GitHub/ClawHub/Claude Marketplace/LobeHub/browse.sh/官方 optional/索引缓存等），并行搜索+trust 分级去重；agentskills.io 只是 SKILL.md 格式标准名。补聚合架构图 | create_source_router :3939 十个 Source 类 |
| ❌ | 排错表"frontmatter 超 4000 截断→技能不出现" | → 技能**仍出现**，但 name 退化为目录名、描述被污染（解析失败回退 skill_dir.name）——故障模式写反 | 审核 Agent 实际构造复现 + 回退逻辑 :663 |
| ❌ | ":1067 异常路径" | → 串行执行路径（execute_tool_calls_sequential :965；单工具轮常走） | def 行验证 |
| ⚠️ | "授予 memory 和 skills 两个工具集" | → skills 恒给、memory 条件给（memory_enabled/user_profile 才给，#54937 修复） | :784-790 注释 |
| ⚠️ | Curator "要求空闲 min_idle_hours" | → 两个真实调用点都传 idle=inf，这道门从未被真实空闲触发；触发者是 gateway housekeeping tick（每 60 tick）+ CLI 启动 | gateway/run.py:19979、cli.py:13173 |
| ⚠️ | "三个安全垫" | → 四个（+PROTECTED_BUILTIN_SKILLS 硬名单，现只有 plan，防斜杠命令无声失效） | skill_usage.py:58-78 |

- **审核确认的高危正确项**：七元组逐项核对（构造行实为 :1458-1466）；digest tail=24；mlops 30 个可选技能按 frontmatter name（非目录名）逐文件核对全部命中——"目录名≠文档名"的假阳性被审核自己排除。
- **⭐ 沉重教训**：**"版本归属必须实测"的备忘规则我自己违反了 5 次**——写作时凭"侦察说是新的 ⇒ v0.18"推断。已在备忘速查表补充这批 commit；后续各章版本词一律先跑 merge-base 再写。另：`git tag --contains | head -1` 会被 backup/* tag 污染，正确姿势是逐 release tag 做 merge-base --is-ancestor。
